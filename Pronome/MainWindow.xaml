<Window x:Class="Pronome.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Pronome"
        mc:Ignorable="d"
        Height="400" Width="800"
        AllowsTransparency="True" WindowStyle="None" Background="Transparent"
        Title="MainWindow">
    <Window.Resources>
        <!--window for displaying options-->
        <local:OptionsWindow
            x:Key="optionsWindow"
            Height="400" Width="800"
            Title="Options"
            SizeToContent="Height">

            <StackPanel>
                <StackPanel.Resources>
                    <Style TargetType="StackPanel">
                        <Setter Property="Margin" Value="10, 0"/>
                    </Style>
                    <Style TargetType="TextBox">
                        <Setter Property="HorizontalAlignment" Value="Left"/>
                    </Style>
                </StackPanel.Resources>
                <Expander Header="Random Muting">
                    <WrapPanel>
                        <StackPanel>
                            <Label Target="randomMuteToggle">Enable</Label>
                            <CheckBox x:Name="randomMuteToggle"/>
                        </StackPanel>
                        <StackPanel>
                            <Label Target="randomMuteInput">Percentage</Label>
                            <TextBox x:Name="randomMuteInput" LostFocus="randomMuteInput_LostFocus" Width="50"/>
                        </StackPanel>
                        <StackPanel>
                            <Label Target="randomMuteTimerInput">Ramp Duration (seconds)</Label>
                            <TextBox x:Name="randomMuteTimerInput" LostFocus="randomMuteTimerInput_LostFocus" Width="50"/>
                        </StackPanel>
                    </WrapPanel>
                </Expander>
            </StackPanel>
        </local:OptionsWindow>
    </Window.Resources>
    <Grid>
        <!--The background element-->
        <Rectangle Fill="DarkBlue" Opacity=".7"/>
        <!--The base panel-->
        <DockPanel>
            <!--Header element. Draggable-->
            <Grid DockPanel.Dock="Top" MouseLeftButtonDown="window_MouseLeftButtonDown">
                <Rectangle Fill="Black" Opacity=".5"/>
                <!--Title Element-->
                <TextBlock Foreground="White" FontSize="30pt" HorizontalAlignment="Center">ProNome</TextBlock>
                <!--Close Window button-->
                <Button HorizontalAlignment="Right" VerticalAlignment="Top" Click="windowCloseButton_Click" Width="23" Height="23" Margin="4">
                    X
                </Button>
            </Grid>

            <!--Main controls-->
            <Grid DockPanel.Dock="Bottom">
                <WrapPanel HorizontalAlignment="Center">
                    <WrapPanel.Resources>
                        <Style TargetType="Button">
                            <Setter Property="Margin" Value="5, 10"/>
                        </Style>
                    </WrapPanel.Resources>
                    <!--Add layer button-->
                    <Button Click="addLayerButton_Click">Add Layer</Button>
                    <!--Play button-->
                    <Button Click="playButton_Click">Play</Button>
                    <!--Pause button-->
                    <Button Click="pauseButton_Click">Pause</Button>
                    <!--Stop button-->
                    <Button Click="stopButton_Click">Stop</Button>
                    <!--Tempo tap button-->
                    <Button Click="tempoTap_Click">Tempo</Button>
                    <!--Tempo input field-->
                    <Grid Margin="5, 0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition/>
                            <ColumnDefinition/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0" VerticalAlignment="Center">
                            <RepeatButton Click="tempoUp_Click" FontSize="6pt">U</RepeatButton>
                            <RepeatButton Click="tempoDown_Click" FontSize="6pt">D</RepeatButton>
                        </StackPanel>
                        <TextBox Name="tempoInput" 
                             LostFocus="tempoInput_LostFocus"
                             Background="Black"
                             Foreground="White"
                             Width="60"
                             VerticalAlignment="Center"
                             Grid.Column="1"/>
                    </Grid>
                    <!--Master volume-->
                    <StackPanel Margin="5, 0">
                        <TextBlock Foreground="White" FontSize="7pt" HorizontalAlignment="Center">Master Vol.</TextBlock>
                        <Slider 
                        Name="masterVolume" 
                        Value="1" 
                        ValueChanged="masterVolume_ValueChanged" 
                        Minimum="0" 
                        Maximum="1" 
                        Width="80"/>
                    </StackPanel>
                    <!--Open options-->
                    <Button Name="openOptionsButton" Click="openOptionsButton_Click">Options</Button>
                </WrapPanel>

                <!--Window Resizer-->
                <TextBlock MouseLeftButtonDown="windowResizer_MouseLeftButtonDown" MouseLeftButtonUp="windowResizer_MouseLeftButtonUp"
                           MouseMove="windowResizer_MouseMove"
                    Foreground="White" Name="windowResizer" VerticalAlignment="Bottom" HorizontalAlignment="Right">
                    resize
                </TextBlock>
            </Grid>

            <!--Layer UI container-->
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel Name="layerStack"/>
            </ScrollViewer>
        </DockPanel>
    </Grid>
</Window>
